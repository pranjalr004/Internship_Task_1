CREATE Table Students(
    StudentID INT PRIMARY KEY AUTO_INCREMENT,
    Name VARCHAR(20),
    Gender VARCHAR(10),
    Age INT,
    Grade varchar(5),
    MathScore INT,
    ScienceScore INT,
    EnglishScore INT
)