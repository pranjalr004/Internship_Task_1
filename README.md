# Internship_Task_1

## Task 1: Database Setup

This task involves creating and setting up the database for the internship project. The database schema and initial data are defined in the `Database_Setup/Database_Creation.sql` file.

### Steps:
1. Run the SQL script in `Database_Setup/Database_Creation.sql` to create the database.
2. Verify the database is set up correctly.
